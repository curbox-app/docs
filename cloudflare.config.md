# Cloudflare Pages build configuration
# These settings are typically configured in the Cloudflare dashboard,
# but are documented here for clarity.
#
# Build command:      npm run build
# Build output dir:   dist
# Root directory:     /
# Node.js version:    20
#
# Environment variables (set in Cloudflare dashboard):
# NODE_VERSION = 20

# ── _headers file (placed in dist/ or public/) ─────────────────────────────
# Security headers are defined in public/_headers below.

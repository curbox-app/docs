---
/**
 * Docs root (curbox.app/docs/) — redirects into the documentation.
 * The marketing landing, blog, and about/contribute pages live on the main
 * curbox.app site; this repo is the documentation subsite only.
 */
import { base } from '../utils/base-url';
return Astro.redirect(`${base}getting-started/what-is-curbox/`);
---
